# В образ едет только исходный код: зависимости ставятся внутри,
# сборка выполняется там же.
node_modules
.next
lib/generated

# Локальное окружение и база разработки
.env
.env.*
!.env.example
*.db
*.db-journal

.git
.github
README.md

# Сырая выгрузка донора нужна только скриптам сборки контента,
# в образе от неё пользы нет: сайт собирается из data/content.
data/donor
